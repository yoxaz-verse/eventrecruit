
import { agencyDataContext } from "@/lib/agency-data";
import { saveRecruitmentPartner } from "@/app/actions/agency-operations";
import { whatsappUrl } from "@/lib/agency-operations";
export default async function PartnersPage(){const ctx=await agencyDataContext();if(!ctx)throw new Error("Partners unavailable.");const {data:partners}=await ctx.db.from("recruitment_partners").select("*").eq("agency_id",ctx.agency.id).order("updated_at",{ascending:false});return <><h1 className="text-4xl font-black">Recruitment partners</h1><p className="mt-2 text-[var(--muted)]">Track which recruiter supplied staff and retain operational accountability.</p><details className="panel mt-6 p-6"><summary className="cursor-pointer text-xl font-black">Add partner</summary><form action={saveRecruitmentPartner} className="mt-5 grid gap-4 sm:grid-cols-2"><label className="label">Agency or recruiter name<input className="input" name="name" required/></label><label className="label">Contact person<input className="input" name="contact_person"/></label><label className="label">Phone<input className="input" name="phone"/></label><label className="label">Email<input className="input" type="email" name="email"/></label><label className="label">Location<input className="input" name="location"/></label><label className="label">Number of staff<input className="input" min="0" type="number" name="staff_count"/></label><label className="label sm:col-span-2">Skills and categories<input className="input" name="skills"/></label><label className="label sm:col-span-2">Previous work<textarea className="input textarea" name="previous_work"/></label><button className="button button-primary sm:col-span-2">Add partner</button></form></details><div className="mt-6 grid gap-4 md:grid-cols-2">{partners?.map(partner=>{const wa=whatsappUrl(partner.phone||"");return <article className="panel p-5" key={partner.id}><span className="badge capitalize">{partner.status}</span><h2 className="mt-3 text-xl font-black">{partner.name}</h2><p>{partner.contact_person||"No contact person"} · {partner.location||"No location"}</p><p className="mt-2 text-sm text-[var(--muted)]">{partner.staff_count} staff · {partner.skills?.join(", ")||"No categories"}</p><div className="mt-4 flex gap-2">{partner.phone?<a className="button button-secondary" href={`tel:${partner.phone}`}>Call</a>:null}{wa?<a className="button button-secondary" href={wa}>WhatsApp</a>:null}{partner.email?<a className="button button-secondary" href={`mailto:${partner.email}`}>Email</a>:null}</div></article>})}{!partners?.length?<p className="panel p-5">No recruitment partners yet.</p>:null}</div></>}
