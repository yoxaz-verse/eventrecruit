import Link from "next/link";import { DashboardShell } from "@/components/dashboard-shell";import { agencyDataContext } from "@/lib/agency-data";
export default async function Notifications(){const ctx=await agencyDataContext();if(!ctx)throw new Error("Notifications unavailable.");const {data}=await ctx.db.from("agency_notifications").select("*").eq("agency_id",ctx.agency.id).order("created_at",{ascending:false});return <DashboardShell active="agency" current="/dashboard/agency/notifications"><h1 className="text-4xl font-black">Notifications</h1><div className="mt-6 grid gap-3">{data?.map(note=><Link href={note.href||"#"} className="panel p-5" key={note.id}><h2 className="font-black">{note.title}</h2><p className="text-sm text-[var(--muted)]">{note.body}</p><time className="mt-2 block text-xs">{new Date(note.created_at).toLocaleString()}</time></Link>)}{!data?.length?<p className="panel p-5">No notifications.</p>:null}</div></DashboardShell>}
