import { AccountSettingsPanel } from "@/components/account-settings-panel";import { DashboardShell } from "@/components/dashboard-shell";import { accountSettingsData } from "@/lib/account-settings";
export default async function Settings(){const data=await accountSettingsData();return <DashboardShell active="agency" current="/dashboard/agency/settings"><div className="mx-auto max-w-3xl"><h1 className="text-4xl font-black">Agency settings</h1><p className="mt-2 mb-6 text-[var(--muted)]">Manage agency notifications, integrations, security, and account controls.</p><AccountSettingsPanel emailNotifications={data.emailNotifications} inAppNotifications={data.inAppNotifications} deletionPending={data.deletionPending}/><section className="panel mt-6 p-6"><h2 className="text-xl font-black">Integration status</h2><p className="mt-2 text-[var(--muted)]">CSV and Excel import plus click-to-chat WhatsApp are active. Google Contacts, Outlook Contacts, device contacts, and WhatsApp Business API remain intentionally disconnected.</p></section></div></DashboardShell>}
