import Link from "next/link";
import { DashboardShell } from "@/components/dashboard-shell";
import { searchAgencyWorkspace } from "@/lib/agency-data";
export default async function SearchPage({searchParams}:{searchParams:Promise<{q?:string}>}){const q=(await searchParams).q??"",results=await searchAgencyWorkspace(q);return <DashboardShell active="agency" current="/dashboard/agency/search"><h1 className="text-4xl font-black">Global search</h1><p className="mt-2 text-[var(--muted)]">Results are limited to records owned by this agency.</p><div className="mt-6 grid gap-3">{results.map(result=><Link className="panel p-5" href={result.href} key={`${result.type}-${result.id}`}><span className="badge">{result.type}</span><h2 className="mt-2 text-xl font-black">{result.title}</h2><p className="text-sm text-[var(--muted)]">{result.detail}</p></Link>)}{q.length>=2&&!results.length?<p className="panel p-5">No results for “{q}”.</p>:null}{q.length<2?<p className="panel p-5">Enter at least two characters in the search bar.</p>:null}</div></DashboardShell>}
