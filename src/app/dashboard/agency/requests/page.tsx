import Link from "next/link";import {DashboardShell} from "@/components/dashboard-shell";import {AgencyClientSelector} from "@/components/agency-client-selector";import {agencyClients,selectedAgencyClient} from "@/lib/agency-workspace";import {createClient} from "@/lib/supabase/server";
export default async function AgencyRequests({searchParams}:{searchParams:Promise<{client?:string}>}){const clients=await agencyClients(),selected=selectedAgencyClient(clients,(await searchParams).client),db=await createClient();if(!db)throw new Error("Requests unavailable.");const {data,error}=selected?await db.from("events").select("id,title,city,staffing_roles(id,title,headcount,status,work_starts_on,work_ends_on)").eq("exhibitor_id",selected.exhibitorId).order("created_at",{ascending:false}):{data:[],error:null};if(error)throw new Error("Unable to load requests.");const roles=(data??[]).flatMap(e=>(e.staffing_roles??[]).map(r=>({...r,event:e})));return <DashboardShell active="agency" current="/dashboard/agency/requests"><h1 className="text-4xl font-black">Client staff requests</h1><AgencyClientSelector clients={clients} selected={selected} path="/dashboard/agency/requests"/>{selected?<Link className="button button-primary mb-6" href={`/dashboard/agency/requests/new?client=${selected.exhibitorId}`}>New staffing request</Link>:null}<div className="grid gap-4">{roles.map(r=><article className="panel p-5" key={r.id}><span className="badge">{r.status}</span><h2 className="mt-3 text-xl font-black">{r.title}</h2><p>{r.event.title} · {r.event.city} · {r.work_starts_on} – {r.work_ends_on}</p><p>{r.headcount} people needed</p></article>)}{selected&&!roles.length?<p className="panel p-5">No staffing requests for this client.</p>:null}</div></DashboardShell>}
