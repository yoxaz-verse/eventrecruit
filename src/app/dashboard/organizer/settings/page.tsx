import { AccountSettingsPanel } from "@/components/account-settings-panel";import { accountSettingsData } from "@/lib/account-settings";
export default async function OrganizerSettings(){const data=await accountSettingsData();return <><div className="mx-auto max-w-3xl"><h1 className="text-4xl font-black">Settings</h1><p className="mt-2 mb-6 text-[var(--muted)]">Manage organizer notifications, security, and account controls.</p><AccountSettingsPanel emailNotifications={data.emailNotifications} inAppNotifications={data.inAppNotifications} deletionPending={data.deletionPending} availability={data.availability}/></div></>}
