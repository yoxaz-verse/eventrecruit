import { submitExhibitorEvent } from "@/app/actions/exhibitor-events";
import { SubmitButton } from "@/components/submit-button";

export function ExhibitorEventForm({exhibitorId}:{exhibitorId?:string}){return <form action={submitExhibitorEvent} className="panel grid gap-5 p-6">{exhibitorId?<input type="hidden" name="exhibitor_id" value={exhibitorId}/>:null}<label className="label">Event title<input className="input" name="title" maxLength={160} required/></label><label className="label">Description<textarea className="input textarea" name="description" maxLength={10000}/></label><div className="grid gap-4 sm:grid-cols-2"><label className="label">Venue<input className="input" name="venue" maxLength={200} required/></label><label className="label">City<input className="input" name="city" maxLength={120} required/></label></div><div className="grid gap-4 sm:grid-cols-2"><label className="label">Starts<input className="input" name="starts_at" type="date" required/></label><label className="label">Ends<input className="input" name="ends_at" type="date" required/></label></div><SubmitButton pendingText="Submitting…">Submit event for approval</SubmitButton></form>}
